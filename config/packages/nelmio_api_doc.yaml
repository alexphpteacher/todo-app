nelmio_api_doc:
  documentation:
    info:
      title: todo.app
      description: This is an awesome todo.app!
      version: 1.0.0

#    paths:
#      /api/login_check:
#        post:
#      /api/task:
#        get:
#      /api/task:
#        post:
#      /api/task/{id}:
#        get:
#      /api/task/{id}:
#        put:
#      /api/task/{id}:
#        delete:

  areas: # to filter documented areas
    path_patterns:
      - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
